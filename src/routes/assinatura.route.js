// / routes/subscriptionRoutes.js
// import express from "express";
// import { createSubscription, getSubscriptionByUser, updateSubscription, deleteSubscription } from "../controllers/subscriptionController.js";

// const router = express.Router();

// // Rota para criar uma assinatura para um profissional
// router.post("/", createSubscription);
// // Rota para buscar detalhes da assinatura de um usuário
// router.get("/:usuarioId", getSubscriptionByUser);
// // Rota para atualizar uma assinatura (mudar plano, por exemplo)
// router.put("/:id", updateSubscription);
// // Rota para cancelar uma assinatura
// router.delete("/:id", deleteSubscription);

// export default router;
